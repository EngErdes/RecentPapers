name: RecentPapers

on:
  schedule:
    - cron: "0 9 * * *"
    
jobs:
  recent-papers:
    name: recent-papers    # 省略可能
    runs-on: ubuntu-latest
    steps:
      - name: exec recent papers 
        env:
          API_KEY: ${{ secrets.NOTION_TOKEN }}
        run: echo "exec recent papers"

        # secretの登録なども必要
      
      
      
    
  
